# ── Web Framework & Server ────────────────────────────────────────────────────
fastapi[standard]
pydantic-settings
uvicorn[standard]

# ── AI Agent Framework ────────────────────────────────────────────────────────
# Slim install — only pull the providers we use (avoids logfire, google, evals)
pydantic-ai-slim[anthropic,openai,mcp]

# ── LLM + Embedding Provider Routing ─────────────────────────────────────────
# All LLM and embedding calls go through LiteLLM — no local model downloads.
# Supports 100+ providers (Anthropic, OpenAI, Ollama, Bedrock, Cohere, etc.)
litellm

# ── Local Embedding (ONNX, runs inside nexus-ai) ──────────────────────────────
# FastEmbed runs bge-m3 via ONNX — no GPU needed, ~300MB, fast CPU inference.
# Switch provider via EMBEDDING_PROVIDER env var (litellm → Ollama/Infinity).
fastembed

# ── Vector Store ──────────────────────────────────────────────────────────────
chromadb                 # Phase 1 — swap via VectorStoreFactory

# ── Dev & Testing ─────────────────────────────────────────────────────────────
rich                     # Better logging
pytest
pytest-asyncio
